// Simulateur de Xmrig JS simplifié
class Xmrig {
  constructor(config) {
    this.config = config;
    this.running = false;
    this.interval = null;
  }

  async start() {
    if (this.config.onOpen) this.config.onOpen();
    this.running = true;
    let count = 0;
    this.interval = setInterval(() => {
      count += Math.floor(Math.random() * 100);
      if (this.config.onUpdate) {
        this.config.onUpdate({ hashesTotal: count });
      }
    }, 1000);
  }

  stop() {
    clearInterval(this.interval);
    this.running = false;
    if (this.config.onClose) this.config.onClose();
  }
}
